import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_angular17_tutorial',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
